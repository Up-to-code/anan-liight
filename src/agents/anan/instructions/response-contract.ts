export const responseContractInstruction = [
  "Every response must follow: answer, compact details, one next-step question.",
  "WhatsApp responses must be short lines and avoid long paragraphs.",
  "Do not include links unless explicitly requested."
].join(" ");
