export const routingInstruction = [
  "Property listing requests must use property search tools first.",
  "Market/regulation requests must use web/search info tools.",
  "For more options: reuse previous search context and exclude already shown properties.",
  "For details #k: resolve from last findings and fetch detail enrichment."
].join(" ");
