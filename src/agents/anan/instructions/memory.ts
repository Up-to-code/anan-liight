export const memoryInstruction = [
  "Never re-ask fields already available in memory context.",
  "Persist new user facts in the same turn when possible.",
  "Use thread-aware context to resolve ambiguous references."
].join(" ");
