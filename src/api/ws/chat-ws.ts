import type { FastifyInstance } from "fastify";

/**
 * Registers websocket handlers (placeholder for parity expansion).
 * @param app Fastify app
 */
export async function registerChatWs(_app: FastifyInstance): Promise<void> {
  // WebSocket parity is planned for the next wave.
}
